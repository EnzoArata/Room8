<div class="row">
  <% if current_user.house != nil  && current_user.house.tasks.count > 0 %>
    <div class="col-lg-5 shadow-lg p-3 mb-5 rounded" id="task-container-1">
      <h1>Upcoming Tasks</h1>
        <div class="dropdown">
          <div class="text-right">
            Sort By
            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Default
            </button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" href="#">Roomate</a>
                <a class="dropdown-item" href="#">Date</a>
                <a class="dropdown-item" href="#">Importance</a>
              </div>
            </div>

          </div>
          <ul class="list-group">
            <% @tasks.each do |task| %>
              <%if !task.completed && task.startDate > Time.now  %>
                <li class="list-group-item list-group-item-light text-left">
                    <%= link_to task.title, task_path(task), class: "text-info" %>
                    <small>Assigned to <%= User.find(task.assignedUser).firstName%> <%= time_ago_in_words( task.startDate) %> from now</small>
                    <div class="text-right">
                      <strong>Due in</strong> <%= time_ago_in_words( task.dueDate) %>




                    </div>

                </li>
              <% end %>
            <% end %>
          </ul>
          <br>

    </div>




    <% else %>
    <% end %>
  </div>
